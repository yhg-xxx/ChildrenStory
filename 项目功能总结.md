# 儿童故事生成平台项目功能总结

## 项目概述

儿童故事生成平台是一个基于AI技术的应用系统，旨在为用户提供智能的儿童故事生成、插图生成以及语音合成功能。系统分为前端和后端两部分，采用前后端分离架构，通过RESTful API进行通信。

## 技术栈

### 前端技术
- **Vue 3**：前端框架
- **Vue Router**：路由管理
- **Element Plus**：UI组件库
- **Axios**：HTTP请求客户端
- **Vite**：构建工具

### 后端技术
- **Spring Boot 3.5.7**：后端框架
- **Spring AI**：AI集成框架
- **MyBatis Plus**：ORM框架
- **MySQL**：数据库
- **百度AI接口**：语音合成和图片生成

## 核心功能模块

### 1. 故事生成功能

#### 前端实现
- 提供关键词输入界面，用户可输入关键词（如：宇航员、小狗、月球等）
- 支持流式获取故事内容，实现实时展示
- 结构化展示故事内容，包括标题、梗概和正文三部分
- 提供取消生成功能

#### 后端实现
- RESTful API接口：`/api/story/generate/stream`
- 使用Spring AI的ChatClient与DeepSeek API交互
- 支持流式响应，实时返回故事内容
- 自动生成故事ID并异步保存到数据库
- 错误处理机制，确保服务稳定性

### 2. 故事插图生成功能

#### 前端实现
- 提供"获取故事插图"按钮
- 异步加载和显示生成的故事插图
- 加载状态提示

#### 后端实现
- RESTful API接口：`/api/story/image/generate/{storyId}`
- 使用百度AI接口进行图片生成
- 支持查询图片生成结果
- 返回生成的图片URL

### 3. 语音合成功能

#### 前端实现
- 提供"生成语音"按钮
- 支持流式接收和实时播放音频
- 音频播放控制
- 资源管理和内存释放

#### 后端实现
- 两种语音合成接口：
  - 非流式：`/api/tts/story/{storyId}`
  - 流式：`/api/tts/story-stream/{storyId}`
- 使用百度TTS服务进行文本转语音
- 音频文件自动保存到uploads目录
- 数据库中保存音频文件URL
- 支持流式传输，实现边生成边播放

### 4. 数据存储功能

- 故事信息存储在MySQL数据库中
- 音频文件存储在uploads目录下
- 支持故事的增删改查操作

## 主要接口列表

### 故事相关接口
- `GET /api/story/generate/stream`：流式生成故事
- `POST /api/story/image/generate/{storyId}`：为故事生成插图
- `GET /api/story/{storyId}`：获取故事详情

### 语音合成接口
- `GET /api/story/{storyId}`：非流式故事语音合成
- `GET /api/story-stream/{storyId}`：流式故事语音合成

## 关键功能特性

1. **流式处理**：故事生成和语音合成均支持流式处理，提供更好的用户体验
2. **错误处理**：完善的错误处理机制，确保系统稳定性
3. **资源管理**：自动管理和释放资源，避免内存泄漏
4. **异步处理**：关键操作采用异步处理，提高系统响应性能
5. **数据持久化**：自动保存生成的故事和相关资源

## 用户体验亮点

1. **实时反馈**：故事生成和语音合成过程中提供实时反馈
2. **结构化展示**：故事内容分标题、梗概和正文结构化展示
3. **多媒体支持**：支持图片和音频，丰富故事表现形式
4. **操作友好**：提供取消、重试等操作，增强用户控制感

## 项目架构特点

1. **前后端分离**：采用现代前后端分离架构，便于维护和扩展
2. **模块化设计**：功能模块清晰分离，职责单一
3. **微服务思想**：各功能模块相对独立，便于后续扩展
4. **异步处理**：关键操作采用异步处理，提高系统吞吐量
5. **流式处理**：多处使用流式处理，提升用户体验

## 总结

儿童故事生成平台结合了AI技术和现代Web开发技术，为用户提供了智能的故事生成、插图生成和语音合成功能。系统架构清晰，功能模块完善，用户体验良好，适合作为儿童教育和娱乐应用使用。